---
name: "github-auth-setup"
description: "Configure GitHub CLI authentication for the current session"
---

To authenticate the GitHub CLI (`gh`) for Git operations, run:

```bash
# Authenticate with GitHub CLI (opens browser or device code flow)
gh auth login

# Verify authentication
gh auth status
```
